# Allowlists And Local Exclusions

This folder contains local controls that remove content from generated outputs.

## Typical Inputs

- host allowlists
- URL regex allowlists
- module-level exclusion identifiers

These files are the correct place to encode repository policy such as application exclusions, false-positive suppression, or local safety constraints.
