# Surge Scripts

This folder contains script assets referenced by published Surge modules.

## Layout

- `Converted/` stores generated compatibility variants when a remote script must be adapted for Surge usage.

Raw third-party scripts are not mirrored here by default. Only repository-controlled generated outputs should be published in this directory.
