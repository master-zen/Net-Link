# Converted Scripts

This folder contains repository-generated script variants produced during compatibility conversion.

These files are created only when a retained remote script requires a Surge-compatible wrapper or shim.
They are outputs of automation and should be regenerated instead of edited manually.
